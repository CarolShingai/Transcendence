const OBSTACLES_CONFIG = {
  baseSpeed: 200,      // Velocidade base de queda (px/s)
  spawnDelay: 2000,    // Intervalo entre spawns (ms)
  maxHarpias: 2,       // Máximo de harpias simultâneas
  maxCarcaras: 3,      // Máximo de carcarás simultâneos
  carcaraSpawnDelay: 1200, // Intervalo de spawn dos carcarás (mais frequente)
};