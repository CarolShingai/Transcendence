// TextureFactory.js não é mais necessário (usando imagens PNG)
class TextureFactory {
  constructor(scene) {
    this.scene = scene;
  }
  generateAll() {
    // Imagens carregadas em GameScene.preload()
  }
}